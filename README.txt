# Gradient Boosting Random Utility model

In this repository, we propose an adaptation of lightGBM with a standalone python file that allows to estimate biogeme models through gradient boosting decision trees.
